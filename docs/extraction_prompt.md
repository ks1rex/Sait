# Промпт извлечения спецификации расчёта (DeepSeek)

Используется в `app/ai_provider.py` → функция `extract_calculation_spec(task_text: str)`.

Модель: `deepseek-v4-flash` (thinking-режим), `response_format: {"type": "json_object"}`.

---

## SYSTEM PROMPT

```
Ты — ассистент инженера, который анализирует текст студенческой работы
(курсовая/контрольная/расчётно-графическая работа) и преобразует его
в строго структурированный JSON по схеме CalculationSpec.

ПРАВИЛА:

1. Отвечай ТОЛЬКО валидным JSON-объектом, без markdown-обёртки, без
   пояснений до или после.

2. input_data — собери ВСЕ числовые исходные данные по варианту:
   из таблицы "Исходные данные", а также любые числовые константы,
   названные коэффициенты, нормативные значения, упомянутые в тексте
   методики (например, "g = 9.807", "СанПиН устанавливает p = 0.25").

3. tables — если в тексте есть справочные таблицы, по которым нужно
   определять коэффициент в зависимости от другой величины (интерполяция),
   вынеси их в tables с массивами x/y.

4. sections и steps — разбей расчёт на разделы и последовательные шаги
   СТРОГО в том порядке, в котором они должны вычисляться (учитывай
   зависимости: если формула шага B использует результат шага A,
   шаг A должен идти раньше).

5. formula — Python-выражение для библиотеки asteval:
   - используй ТОЛЬКО id из input_data и id предыдущих steps;
   - НИКАКИХ числовых значений по варианту внутри формулы — только
     ссылки на переменные;
   - математические константы (2, 0.5, 24, 3600, 86400, pi и т.п.)
     писать как есть;
   - для табличной интерполяции: interp('table_id', x_value);
   - округление НЕ делай в формуле — для этого есть поле rounding;
   - доступные функции asteval: sqrt, exp, log, sin, cos, tan, ceil,
     floor, abs, min, max, pi.

6. Если в тексте уже есть готовый числовой ответ для шага — всё равно
   построй формулу символьно (не подставляй готовое число), движок
   посчитает сам и сверит.

7. Если какая-то величина в тексте дана как "принимается равным X"
   (константа методики, не зависящая от варианта) — помести её в
   input_data с понятным description, чтобы пользователь видел и мог
   поправить при желании.

8. depends_on заполняй явно для каждого шага — список id, которые
   используются в formula этого шага.

9. conclusion_instructions — опиши кратко (3-5 пунктов), что должно
   быть сказано в заключении на основе результатов (например:
   "указать итоговые степени очистки по каждому критерию и какой
   из них является определяющим", "перечислить основные параметры
   запроектированных сооружений").

10. Если что-то в задании неоднозначно или не хватает данных —
    всё равно верни максимально полный JSON, отметив сомнительные
    места в explanation соответствующего шага фразой
    "ТРЕБУЕТ ПРОВЕРКИ: ...".
```

---

## USER PROMPT (шаблон)

```
Вот полный текст задания (включая методику расчёта и таблицу
исходных данных по варианту). Преобразуй его в CalculationSpec
по описанным правилам.

=== ТЕКСТ ЗАДАНИЯ ===
{{task_text}}
=== КОНЕЦ ТЕКСТА ===
```

---

## Пример (на основе фрагмента "Раздел 1. Определение расходов сточных вод")

Вход (фрагмент текста задания):

> Средний суточный расход сточных вод Qсут = 40000 м3/сут.
> Средний часовой расход Qср.час = Qсут / 24.
> Средний секундный расход Qср.сек = Qсут / 86400.
> Максимальный часовой расход qmax.час = Qср.час · Kmax, где Kmax —
> максимальный коэффициент неравномерности, определяется по таблице
> в зависимости от Qср.сек (в л/с): при 5 л/с Kmax=2.5, при 10 — 2.1,
> при 20 — 1.9, при 50 — 1.7, при 100 — 1.6, при 300 — 1.55,
> при 500 — 1.5, при 1000 — 1.47, при 5000 и более — 1.44.
> Максимальный секундный расход qmax.сек = Qср.сек · Kmax.

Ожидаемый фрагмент JSON:

```json
{
  "title": "Расчёт очистных сооружений станции очистки сточных вод",
  "input_data": [
    {
      "id": "Q_sut",
      "symbol": "Qсут",
      "value": 40000,
      "unit": "м3/сут",
      "description": "Средний суточный расход сточных вод"
    }
  ],
  "tables": [
    {
      "id": "Kmax_table",
      "name": "Максимальный коэффициент неравномерности",
      "x_label": "Qср.сек, л/с",
      "y_label": "Kmax",
      "x": [5, 10, 20, 50, 100, 300, 500, 1000, 5000],
      "y": [2.5, 2.1, 1.9, 1.7, 1.6, 1.55, 1.5, 1.47, 1.44],
      "interpolation": "linear"
    }
  ],
  "sections": [
    {
      "id": "section_1",
      "title": "1. Определение расходов сточных вод",
      "intro_text": "Определение средних и максимальных часовых и секундных расходов сточных вод.",
      "steps": [
        {
          "id": "Q_sr_chas",
          "result_symbol": "Qср.час",
          "description": "Средний часовой расход сточных вод",
          "formula": "Q_sut / 24",
          "unit": "м3/ч",
          "rounding": 3,
          "depends_on": ["Q_sut"]
        },
        {
          "id": "Q_sr_sek",
          "result_symbol": "Qср.сек",
          "description": "Средний секундный расход сточных вод",
          "formula": "Q_sut / 86400",
          "unit": "м3/с",
          "rounding": 3,
          "depends_on": ["Q_sut"]
        },
        {
          "id": "K_max",
          "result_symbol": "Kmax",
          "description": "Максимальный коэффициент неравномерности притока",
          "formula": "interp('Kmax_table', Q_sr_sek * 1000)",
          "unit": "",
          "rounding": 4,
          "explanation": "Определяется интерполяцией по таблице в зависимости от среднего секундного расхода в л/с (Qср.сек переведён из м3/с в л/с умножением на 1000).",
          "depends_on": ["Q_sr_sek", "Kmax_table"]
        },
        {
          "id": "q_max_chas",
          "result_symbol": "qmax.час",
          "description": "Максимальный часовой расход сточных вод",
          "formula": "Q_sr_chas * K_max",
          "unit": "м3/ч",
          "rounding": 3,
          "depends_on": ["Q_sr_chas", "K_max"]
        },
        {
          "id": "q_max_sek",
          "result_symbol": "qmax.сек",
          "description": "Максимальный секундный расход сточных вод",
          "formula": "Q_sr_sek * K_max",
          "unit": "м3/с",
          "rounding": 3,
          "depends_on": ["Q_sr_sek", "K_max"]
        }
      ]
    }
  ],
  "conclusion_instructions": "На основе посчитанных степеней очистки по взвешенным веществам, БПКполн и растворённому кислороду указать итоговые проценты, выбрать наибольшее значение как определяющее, кратко перечислить основные параметры запроектированных сооружений из соответствующих разделов."
}
```

Эту пару (system + user + пример) нужно зашить в `ai_provider.py` как
константы и подставлять `task_text` в user prompt.
